# 收录与排名规则

## 收录目标

每天发布少量、来源可追溯、普通用户能够理解并实际使用的 Agent Skills。

## 入选条件

- 存在可读取的 `SKILL.md` 或等价 Agent Skill 包。
- 有原始仓库、精确 Skill 路径和来源链接。
- 能说明普通用户可以获得的具体结果。
- 安装、账号、费用、权限和数据流向没有刻意隐瞒。
- 普通用户适配评分不低于 60。
- 状态不是 `failed` 或 `security_risk`。

## 排名原则

- 优先使用 Skill 级安装量、近期增长和独立讨论。
- 累计仓库 Stars 只作辅助，不能代表每个 Skill 的热度。
- 同一仓库每天默认最多入选 2 项。
- 热度依据必须公开展示，无法验证的信号不会写成“热门”。
- `install` 表示完成了更充分的安装、功能和风险验证。
- `test` 表示值得尝试，但使用者仍需阅读原始说明。

## 不收录

- 只有提示词、README、MCP 服务、插件、框架、API 或 CLI，没有 Skill 包装。
- 正常使用需要专业资质、开发服务器、云基础设施或编程能力。
- 来源无法追踪、复制无归属、安装方式不可确认。
- 请求危险权限但没有解释用途。
- 只有累计 Stars，没有 Skill 级使用或增长信号。

## 内容原则

- 大众化简介必须基于原始 Skill 页面，不根据名称凭空生成。
- 不无依据声称免费、隐私安全、一键安装或兼容某个平台。
- 每条保留来源、热度依据、普通用户适配度和风险提示。
- 历史榜单只追加；更正时保留可追溯记录。

## 安全边界

- 采集过程只读取第三方 Skill，不直接执行第三方脚本。
- 榜单推荐不等于安全认证。
- 用户安装前应自行检查源码、权限、账号和付费要求。
